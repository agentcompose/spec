name: release

# Publishes to npm when a GitHub Release is published.
# Bootstrap auth: a granular automation token in the NPM_TOKEN secret.
on:
  release:
    types: [published]

permissions:
  contents: read
  id-token: write # required for provenance attestation

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"
      - run: npm install
      - run: npm run validate
      - run: npm publish --access public --provenance
        # Tokenless publish via OIDC trusted publishing (configure the trusted
        # publisher for @agentcompose/spec on npmjs.com once, then no secret is needed).
