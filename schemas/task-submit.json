{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agentcompose.dev/schemas/task-submit.json",
  "title": "Task Submit Params",
  "description": "Parameters for the tasks/submit method: a goal expressed as input parts.",
  "type": "object",
  "required": ["goal"],
  "additionalProperties": false,
  "properties": {
    "capabilityId": {
      "type": "string",
      "description": "Optional hint selecting which capability should handle the goal."
    },
    "goal": {
      "type": "array",
      "minItems": 1,
      "description": "The desired outcome, expressed as one or more input parts.",
      "items": { "$ref": "https://agentcompose.dev/schemas/common.json#/$defs/Part" }
    },
    "metadata": {
      "type": "object",
      "additionalProperties": true,
      "description": "Optional caller-supplied metadata (e.g. trace ids, priorities)."
    },
    "extensions": { "$ref": "https://agentcompose.dev/schemas/common.json#/$defs/Extensions" }
  }
}
